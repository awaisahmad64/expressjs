# expressjs
```
$ npx express-generator --veiw=ejs express-generator
#change directory
$ cd express-generator
#install dependencies
$ npm install
#run the app
$ DEBUG=express-generator:* npm start
```
